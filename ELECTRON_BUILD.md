# Сборка нативного приложения HARDY для Windows

## Требования
- Node.js 18+
- Git
- GitHub аккаунт (для автообновлений)

## Инструкция по сборке

### 1. Экспортируй проект в GitHub
В Lovable: **Settings → GitHub → Export to GitHub**

### 2. Склонируй репозиторий
```bash
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

### 3. Настрой автообновления
Открой файл `electron-builder.json` и замени:
```json
"publish": {
  "provider": "github",
  "owner": "YOUR_GITHUB_USERNAME",  // <- твой username
  "repo": "YOUR_REPO_NAME",          // <- название репозитория
  "releaseType": "release"
}
```

### 4. Установи зависимости
```bash
npm install
```

### 5. Собери веб-приложение
```bash
npm run build
```

### 6. Собери Windows приложение
```bash
npx electron-builder --win
```

### 7. Готово!
Файлы появятся в папке `release/`:
- `HARDY Кодексы-X.X.X-Setup.exe` — установщик
- `HARDY Кодексы-X.X.X-Portable.exe` — портативная версия

---

## 🔄 Автообновления

Приложение автоматически:
- Проверяет обновления при запуске
- Проверяет каждые 30 минут
- Показывает диалог при наличии новой версии
- Скачивает и устанавливает при согласии

### Публикация обновления

1. Измени версию в `package.json`:
```json
"version": "1.0.1"
```

2. Собери и опубликуй:
```bash
npm run build
npx electron-builder --win --publish always
```

3. На GitHub появится новый Release с `.exe` файлами

4. Все пользователи получат уведомление об обновлении!

### Ручная проверка обновлений
Правый клик по иконке в трее → **"Проверить обновления"**

---

## Глобальные горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl+Shift+H` | Открыть HARDY и юридического помощника |

> ⚠️ Используется `Ctrl+Shift+H` вместо `Ctrl+F`, чтобы не конфликтовать с другими программами.

---

## Трей (системный лоток)

Приложение сворачивается в трей при закрытии:
- **Клик** по иконке — открыть приложение
- **Правый клик** — меню:
  - Открыть HARDY
  - Юридический помощник
  - Проверить обновления
  - Выход

---

## Для разработки

Запуск в режиме разработки:
```bash
# Терминал 1: запустить веб-сервер
npm run dev

# Терминал 2: запустить Electron
npx electron electron/main.js
```

---

## Структура файлов Electron

```
electron/
├── main.js      # Главный процесс Electron (+ автообновления)
└── preload.js   # Preload скрипт
electron-builder.json  # Конфигурация сборки и публикации
```

---

## GitHub Token (опционально)

Для автоматической публикации релизов создай токен:
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token с правами `repo`
3. При сборке:
```bash
GH_TOKEN=your_token npx electron-builder --win --publish always
```
